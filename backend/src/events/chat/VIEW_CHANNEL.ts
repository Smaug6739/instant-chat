import { IObject } from "../../types"

// import { auth } from '../auth'
export async function run(server: any, socket: any, channel: IObject) {

	//socket.join(`01${channel.id}`)
}

const infos = {
	event: 'VIEW_CHANNEL'
}

export { infos }